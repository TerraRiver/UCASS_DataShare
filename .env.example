# ==================================================
# UCASS DataShare - Docker 部署环境变量配置
# ==================================================
#
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 根据你的实际环境修改以下配置
# 3. 生产环境务必修改所有密码和密钥！
#

# ==================================================
# 数据库配置
# ==================================================

# PostgreSQL 数据库名称
POSTGRES_DB=ucass_datashare

# PostgreSQL 用户名
POSTGRES_USER=ucass_datashare

# PostgreSQL 密码（⚠️ 生产环境务必修改！）
POSTGRES_PASSWORD=Ww2368963068

# PostgreSQL 端口（宿主机访问端口）
POSTGRES_PORT=5432

# ==================================================
# 应用配置
# ==================================================

# JWT 密钥（⚠️ 生产环境务必使用强密钥！）
# 可以使用以下命令生成：openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 管理员账号（⚠️ 生产环境务必修改！）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# 文件上传大小限制（字节）
# 默认：10GB = 10737418240 字节
MAX_FILE_SIZE=10737418240

# ==================================================
# 端口配置
# ==================================================

# HTTP 端口（外部访问端口）
HTTP_PORT=80

# HTTPS 端口（如果启用 SSL）
HTTPS_PORT=443

# ==================================================
# 可选配置
# ==================================================

# Node 环境
NODE_ENV=production

# 时区设置
TZ=Asia/Shanghai

# ==================================================
# SSL/HTTPS 配置（可选）
# ==================================================
# 如果需要启用 HTTPS，请取消注释以下配置
# 并在 nginx/ssl 目录下放置证书文件

# SSL_CERTIFICATE=/etc/nginx/ssl/fullchain.pem
# SSL_CERTIFICATE_KEY=/etc/nginx/ssl/privkey.pem
